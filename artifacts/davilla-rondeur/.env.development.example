# Surcharge locale optionnelle (gitignoré si .env.development.local)
# Boutique (pnpm dev) : VITE_API_BASE_URL=https://api.davilla-rondeur.fr
# Portail (pnpm dev:portail) : pas de base URL — /api proxifié vers l'API prod
# API locale : VITE_API_BASE_URL=http://localhost:8080
